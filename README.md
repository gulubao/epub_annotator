
# run

```bash
source .venv/bin/activate && python main.py "<input_epub_path>" -o "<output_epub_path>"
```

# Installation

```bash
source .venv/bin/activate && uv pip install -r requirements.txt
```

字典下载: [[data/README.md]]